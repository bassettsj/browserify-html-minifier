/*global describe, it */
'use strict';
var assert = require('assert');
var browserifyHtmlMinifier = require('../');

describe('browserify-html-minifier node module', function () {
  it('must have at least one test', function () {
    browserifyHtmlMinifier();
    assert(false, 'I was too lazy to write any tests. Shame on me.');
  });
});
